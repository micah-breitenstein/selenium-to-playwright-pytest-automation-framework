[pytest]
testpaths = tests
addopts = -q --tb=short

markers =
    no_safari: skip test when running under Safari (e.g. CDP-only features)

log_cli = false
log_cli_level = INFO
log_cli_format = %(asctime)s | %(levelname)s | %(name)s | %(message)s
log_cli_date_format = %H:%M:%S

